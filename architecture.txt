┌─────────────────────────────────────────────────────────────────────────────┐
│                          Planner 整体架构（4 域版）                            │
└─────────────────────────────────────────────────────────────────────────────┘

  用户 query + memory
       │
       ▼
┌──────────────────────────────────────────────────────────────────────────────┐
│  ⓪ 意图拆分 (Intent Split)  —— 可选阶段（plan_multi 入口）                    │
│  ┌─────────────┐    guided_json     ┌──────────────────────────────┐        │
│  │  vLLM 推理   │◄─────────────────── │ intent_split_schema          │        │
│  └──────┬──────┘                    └──────────────────────────────┘        │
│         │ {multi: bool, sub_queries: [...]}                                  │
│         │ 单意图 → 直接走 plan()；多意图 → 并发 plan(sub_q[i])               │
└─────────┼────────────────────────────────────────────────────────────────────┘
          │
          ▼
┌──────────────────────────────────────────────────────────────────────────────┐
│  ① 路由阶段 (Route)                                                          │
│  ┌─────────────┐    guided_json     ┌──────────────────────────────┐        │
│  │  vLLM 推理   │◄─────────────────── │ route_schema (小schema)      │        │
│  └──────┬──────┘                    └──────────────────────────────┘        │
│         │ {domain, intent, tool, confidence}                                 │
│         │                                                                    │
│         ├─ domain=vod/educ  ──► ② IR 生成                                    │
│         ├─ domain=audio     ──► ②A 有声 slot-fill                            │
│         ├─ domain=device    ──► ②B 设备 slot-fill                            │
│         └─ 慢链路/简单工具   ──► 直接输出（只传 query 原文）                     │
└─────────┼────────────────────────────────────────────────────────────────────┘
          │
          ▼ (按 domain 分流)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
分支 A：vod / educ —— IR 编译管线
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

┌──────────────────────────────────────────────────────────────────────────────┐
│  ② IR 生成阶段                                                               │
│                                                                              │
│  ┌───────────────────────────────────────────┐                               │
│  │        Experience Bank (Prompt层)          │                               │
│  │  ┌─────────────────────────────────────┐  │                               │
│  │  │  Rule Store (27条结构化规则)          │  │                               │
│  │  │  ┌───────┬───────┬───────┬─────┐   │  │                               │
│  │  │  │action │ tag   │field  │sort │...│  │                               │
│  │  │  │规则    │规则    │选择   │规则  │   │  │                               │
│  │  │  └───────┴───────┴───────┴─────┘   │  │                               │
│  │  └──────────────────┬──────────────────┘  │                               │
│  │                     │ 全量注入（规则<30）    │                               │
│  └─────────────────────┼─────────────────────┘                               │
│                        │                                                      │
│                        ▼                                                      │
│  ┌─────────────────────────────────────────────────────────┐                 │
│  │  IR Prompt = 基础指令 + 规则 + 21组few-shot + query       │                 │
│  └───────────────────────────┬─────────────────────────────┘                 │
│                              │                                                │
│  ┌───────────────────────────▼────────────────────────────┐                  │
│  │  vLLM 推理 (guided_json = IR schema by domain)          │                  │
│  │  字段枚举由 registry 按 domain 收紧 → 消除字段幻觉         │                  │
│  └───────────────────────────┬────────────────────────────┘                  │
│                              │ 域无关布尔 IR                                   │
│                              ▼                                                │
│  ┌────────────────────────────────────────┐                                  │
│  │  validate_ir  ──失败──► 回灌自修复(≤2次) │                                  │
│  └───────────────────────────┬────────────┘                                  │
└──────────────────────────────┼───────────────────────────────────────────────┘
                               │ 合法 IR
                               ▼
┌──────────────────────────────────────────────────────────────────────────────┐
│  ③ 编译 + 后处理阶段 (Compiler)                                               │
│                                                                              │
│  ┌────────────────────────────────────────────────────────────────────┐      │
│  │  双后端编译:                                                        │      │
│  │  ├─ compile_nested → vod_search / vod_search_all / educ_search     │      │
│  │  ├─ compile_relate → vod_relate_search / educ_relate_recommend     │      │
│  │  └─ compile_flat   → *_slow_search（只传query原文）                 │      │
│  │                                                                    │      │
│  │  工具自动选择：IR 字段子集 ⊆ VOD_SEARCH_FIELDS → vod_search        │      │
│  │                否则 → vod_search_all                                │      │
│  └────────────────────────────┬───────────────────────────────────────┘      │
│                               │                                              │
│                               ▼                                              │
│  ┌────────────────────────────────────────────────────────────────────┐      │
│  │  Experience Bank (Compiler层) —— 确定性后处理，全量执行               │      │
│  │                                                                    │      │
│  │  ┌──────────────┐ ┌──────────────┐ ┌──────────────┐               │      │
│  │  │ 值归一化      │ │ 字段 remap   │ │ 结构简化      │               │      │
│  │  │ ·人名间隔符   │ │ ·好莱坞→comp │ │ ·and解包     │               │      │
│  │  │ ·vender大小写 │ │ ·卫视→channel│ │ ·not fee→0   │               │      │
│  │  │ ·tag/company  │ │ ·TVB→company │ │ ·operator补全 │               │      │
│  │  └──────────────┘ └──────────────┘ └──────────────┘               │      │
│  │                                                                    │      │
│  │  ┌──────────────┐ ┌──────────────┐ ┌──────────────┐               │      │
│  │  │ query-text   │ │ sort 控制    │ │ 时间/名称     │               │      │
│  │  │ ·影片→电影   │ │ ·好看的→删   │ │ ·去年→年份   │               │      │
│  │  │ ·韩剧→拆分   │ │ ·有分数→删   │ │ ·奖项归一   │               │      │
│  │  │ ·节目→综艺   │ │ ·最近→new   │ │ ·title拆数字 │               │      │
│  │  └──────────────┘ └──────────────┘ └──────────────┘               │      │
│  │                                                                    │      │
│  │  ┌──────────────────────────────────────────┐                      │      │
│  │  │ action 动词覆盖 (最后执行)                 │                      │      │
│  │  │ ·强动词(播放/放/请播/打开) → play          │                      │      │
│  │  │ ·弱动词(看/想看)+有title → play            │                      │      │
│  │  │ ·弱动词+无title / 无动词 → search          │                      │      │
│  │  └──────────────────────────────────────────┘                      │      │
│  └────────────────────────────────────────────────────────────────────┘      │
│                               │                                              │
│                               ▼                                              │
│                    {tool_name, parameters}                                    │
└──────────────────────────────────────────────────────────────────────────────┘

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
分支 B：audio —— 有声 Slot-Fill 管线
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

┌──────────────────────────────────────────────────────────────────────────────┐
│  ②A 有声 slot-fill                                                           │
│  ┌─────────────┐    guided_json     ┌──────────────────────────────┐        │
│  │  vLLM 推理   │◄─────────────────── │ audio_schema                 │        │
│  └──────┬──────┘                    │ {tool, query, play_mode,     │        │
│         │                           │  screen_mode}                 │        │
│         ▼                           └──────────────────────────────┘        │
│  ┌────────────────┐                                                          │
│  │ compile_audio  │ → (tool_name, {query, play_mode, screen_mode})           │
│  └────────────────┘                                                          │
│                                                                              │
│  工具：audio_search / audio_chat_qa                                           │
│  play_mode：search / play / screen_off_play                                  │
│  screen_mode：normal / screen_standby                                        │
└──────────────────────────────────────────────────────────────────────────────┘

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
分支 C：device —— 设备控制 Slot-Fill + EB Compiler 管线
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

┌──────────────────────────────────────────────────────────────────────────────┐
│  ②B 设备 slot-fill                                                           │
│  ┌─────────────┐    guided_json     ┌──────────────────────────────┐        │
│  │  vLLM 推理   │◄─────────────────── │ device_schema                │        │
│  └──────┬──────┘                    │ tool(17选1, enum约束)          │        │
│         │                           │ ┌─常规(16工具)─────────────┐  │        │
│         │                           │ │ operation / object /     │  │        │
│         │                           │ │ value / date_time        │  │        │
│         │                           │ └──────────────────────────┘  │        │
│         │                           │ ┌─AI音画诊断(1工具)────────┐  │        │
│         │                           │ │ intent (18值enum)        │  │        │
│         │                           │ └──────────────────────────┘  │        │
│         ▼                           └──────────────────────────────┘        │
│  ┌──────────────────────────────────────────────────────────────────────┐    │
│  │ compile_device — EB Compiler 层（确定性后处理）                         │    │
│  │                                                                      │    │
│  │  ┌─────────────────────────────────────────────────────────────┐     │    │
│  │  │ ① Tool 纠偏（精确→value→query子串→模糊→关键词 五级匹配）      │     │    │
│  │  │    object→tool 精确词表 / value→tool / query最长子串 /        │     │    │
│  │  │    object模糊近邻 / 高置信关键词兜底                           │     │    │
│  │  └─────────────────────────────────────────────────────────────┘     │    │
│  │                                                                      │    │
│  │  ┌─────────────────────────────────────────────────────────────┐     │    │
│  │  │ ② 参数归一化（按工具分支）                                     │     │    │
│  │  │                                                             │     │    │
│  │  │  mode_control:                                              │     │    │
│  │  │    · object同义词(画质模式→图像模式, 音质模式→声音模式)        │     │    │
│  │  │    · value是类别词时strip(声音场景模式→obj=声音模式,val="")    │     │    │
│  │  │    · 模式值拆分(obj=标准模式→obj=图像模式,val=标准模式)        │     │    │
│  │  │    · value→object映射(杜比音效→音效模式, 体育模式→图像模式)    │     │    │
│  │  │    · 纯类别无value时 operation=打开                           │     │    │
│  │  │                                                             │     │    │
│  │  │  source_switch:                                             │     │    │
│  │  │    · 非信号源设备(机顶盒/数字电视) → op=打开,obj=设备名       │     │    │
│  │  │    · 具体信号源(hdmi1/vga) → op=设置,obj=信号源,val=源名     │     │    │
│  │  │    · HDMI无编号/格式归一                                     │     │    │
│  │  │                                                             │     │    │
│  │  │  playback_control:                                          │     │    │
│  │  │    · operation归一(跳转→快进到/快退到 query感知)              │     │    │
│  │  │    · 停止→暂停; 上一个→上+value=集                           │     │    │
│  │  │    · 时间格式(01:30→1:30); 时长(2分钟→2分)                   │     │    │
│  │  │    · 播放模式(value=列表播放→op=列表播放+obj=播放列表)         │     │    │
│  │  │                                                             │     │    │
│  │  │  network_control:                                           │     │    │
│  │  │    · 精确同义词归一(wifi网络→无线网络, 以太网→有线网络)        │     │    │
│  │  │    · 保持特定功能名不过度归一(wifi全时推送等)                  │     │    │
│  │  │    · operation: 设置/启动→打开                               │     │    │
│  │  │                                                             │     │    │
│  │  │  screen_layout: 小屏→obj=小屏; 分屏/全屏→归一+打开           │     │    │
│  │  │  numeric_adjust: 静音→打开; 音量最大→100%; 去"格"             │     │    │
│  │  │  power_control: 开机/关机/重启→打开                           │     │    │
│  │  │  display/audio_control: 设置/启动→打开                        │     │    │
│  │  │  timer_control: 定时关机→关机                                 │     │    │
│  │  │  通用: value-based重路由后参数修正; 英文小写; 空字段省略        │     │    │
│  │  └─────────────────────────────────────────────────────────────┘     │    │
│  └──────────────────────────────────────────────────────────────────────┘    │
│                               │                                              │
│                               ▼                                              │
│                    {tool_name, parameters}                                    │
└──────────────────────────────────────────────────────────────────────────────┘


┌──────────────────────────────────────────────────────────────────────────────┐
│  数据流向总结                                                                 │
│                                                                              │
│  query ──► [意图拆分] ──► Route ──┬── vod/educ ──► IR(+EB) ──► 编译 ──► 输出 │
│                                   ├── audio ──► slot-fill ──► compile ──► 输出│
│                                   ├── device ──► slot-fill ──► EB+compile ► 输出│
│                                   └── 慢链路/简单 ──────────────────────► 输出 │
│                                                                              │
│  Experience Bank 层次:                                                        │
│    Prompt层 (vod/educ): 27条规则 + 21组few-shot → 指导模型 IR 生成             │
│    Prompt层 (device): 10组few-shot → 指导模型 slot-fill                       │
│    Compiler层 (vod/educ): 编译后确定性修正（值归一/结构简化/sort/title/time）   │
│    Compiler层 (device): tool纠偏 + 参数归一化（mode/source/playback/network）  │
└──────────────────────────────────────────────────────────────────────────────┘


┌──────────────────────────────────────────────────────────────────────────────┐
│  Registry 驱动的约束解码                                                      │
│                                                                              │
│  registry.py (单一事实源)                                                     │
│    │                                                                         │
│    ├─► grammar.py: build_route_schema / build_ir_schema(domain)              │
│    │               build_audio_schema / build_device_schema                   │
│    │               build_intent_split_schema                                  │
│    │                                                                         │
│    ├─► compiler.py: compile_nested / compile_flat / compile_audio             │
│    │                compile_device / compile_with_fallback                    │
│    │                device_object_tool_map (schema CSV 解析)                  │
│    │                                                                         │
│    └─► ir.py: validate_ir (字段合法性、类型、域归属)                            │
│                                                                              │
│  字段/工具变更 → 只改 registry.py → schema/编译器/校验自动同步                  │
└──────────────────────────────────────────────────────────────────────────────┘


┌──────────────────────────────────────────────────────────────────────────────┐
│  设备控制域：17 工具 + 2 参数范式（0731 schema）                               │
│                                                                              │
│  常规控制（16 工具）：                                                        │
│    numeric_adjust | power_control | timer_control | source_switch             │
│    playback_control | video_picture_save_share | mode_control                 │
│    screen_layout | screen_lift_rotation | demo_control                        │
│    display_control | audio_control | smart_camera | network_control           │
│    screensaver_control | common_control                                      │
│    → 参数: {operation, object, [value], [date_time]}                         │
│                                                                              │
│  AI 音画诊断（1 工具）：                                                      │
│    solve_picture_sound_problem_control                                       │
│    → 参数: {intent}（18 种画质/音效异常诊断枚举）                               │
│                                                                              │
│  tool 纠偏词表：object→tool 精确表(自动从 schema CSV 解析)                     │
│  + value→tool / query子串 / 模糊近邻 / 关键词兜底                             │
└──────────────────────────────────────────────────────────────────────────────┘


┌──────────────────────────────────────────────────────────────────────────────┐
│  评测体系                                                                     │
│                                                                              │
│  bench_vod.py（影视）:                                                        │
│    · 多worker并发请求 vLLM                                                    │
│    · param_diff 差异分析列                                                    │
│    · 按工具细分统计                                                            │
│                                                                              │
│  bench_device.py（设备）:                                                     │
│    · 工具别名归一（canonical_device_tool）                                     │
│    · 空字段对称容忍（value=""/device=""/location="" 视为不存在）                │
│    · 评测容忍规则：                                                            │
│      - value="默认" → 视为空（评测集标注占位符）                                │
│      - 时间去前导零（01:30 ≡ 1:30）                                           │
│      - 时长格式（2分 ≡ 2分钟）                                                │
│      - operation 同义词（开启/启动 ≡ 打开）                                    │
│    · param_diff 差异列 + trace JSONL 输出                                     │
│                                                                              │
│  export_device_eval_issues.py:                                               │
│    · 扫描评测集自身标注不一致                                                  │
│    · 输出返修清单 CSV                                                          │
└──────────────────────────────────────────────────────────────────────────────┘


┌──────────────────────────────────────────────────────────────────────────────┐
│  Experience Bank RAG 检索策略（规划中，当前全量注入）                            │
│                                                                              │
│  ┌────────────┐         ┌────────────────────────┐                           │
│  │ 用户 query  │────────►│  阶段1: 关键词/正则匹配  │  <1ms                    │
│  └────────────┘         │  (trigger词表精确命中)   │                           │
│                         └───────────┬────────────┘                           │
│                                     │ 命中数 < top_k?                         │
│                                     ▼                                        │
│                         ┌────────────────────────┐                           │
│                         │  阶段2: 向量语义检索     │  ~5ms                     │
│                         │  (embedding cosine sim) │                           │
│                         └───────────┬────────────┘                           │
│                                     │                                        │
│                                     ▼                                        │
│                         ┌────────────────────────┐                           │
│                         │  输出: 3-5条规则        │                           │
│                         │  + 对应 few-shot 示例   │                           │
│                         └────────────────────────┘                           │
│                                                                              │
│  适用阈值:                                                                    │
│    规则 <30条  → 全量注入（当前 vod/educ 27条规则）                              │
│    规则 30-100 → 关键词粗筛                                                    │
│    规则 100+   → 向量检索                                                      │
└──────────────────────────────────────────────────────────────────────────────┘
